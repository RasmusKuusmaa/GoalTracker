# Goal Tracker

A goal and habit tracking app: long-term goals live in an arbitrary-depth tree, recurring
commitments are tracked separately and optionally linked to a goal, and daily logging builds
per-commitment streaks. The backend is in `backend/`, the Flutter client is in `app/`.
